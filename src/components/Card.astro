<div class="card">
  <slot />
</div>

<style>
  .card {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 24px;
    height: 410px;
    padding: 16px 32px;
    border: 1px solid hsl(var(--border));
    border-radius: var(--radius);
  }
</style>
